# Development-only dependencies.
# Runtime deps live in requirements.txt and are NOT duplicated here -
# CI installs both files in sequence (runtime first, then dev).
pytest>=9.1.1,<10.0
pytest-cov>=7.1.0,<8.0
ruff>=0.16.5,<1.0
